/** @type {import('tailwindcss').Config} */
export default {
  // Note: Tailwind v4 uses @variant dark in CSS for dark mode.
  // This file is kept for reference; actual config is in src/index.css.
  darkMode: 'class',
  content: ['./index.html', './src/**/*.{js,jsx,ts,tsx}'],
}
